import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitor.fcfm',
  appName: 'ionic-basic-1915003',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
