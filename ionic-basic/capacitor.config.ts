import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitordemo.fcfm',
  appName: 'Ionic-Basic-1954387',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
